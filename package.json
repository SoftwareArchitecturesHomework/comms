{
  "scripts": {
    "setup": "mix deps.get && mix compile",
    "server-iex": "iex -S mix phx.server",
    "test": "mix test",
    "list-mix-tasks": "mix help",
    "rsa-key-gen": "ssh-keygen -t rsa -b 4096 -m PEM -f private.key",
    "rsa-pub-key-gen": "ssh-keygen -f private.key -e -m PKCS8 > public.key",
    "docker-build": "docker build . -t wingsmc/comms:latest",
    "ngrok:publish": "ngrok http 4000"
  }
}
